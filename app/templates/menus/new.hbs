<div class="row pt-4 pb-2">
  <div class="col-3">
    <h2>Create Menu for</h2>
  </div>
  <div class="col-4">
    {{#power-select options=weeks disabled=weekSaved selected=(find-by 'id' weekDestination weeks)  placeholder="Select a Week" onchange=(action "changeWeek") as |week|}}
      {{week.value}}
    {{/power-select}}
  </div>
  <div class="col">
    {{link-to "Compute Shopping List" "menus.list" 1 class="btn float-right btn-lg d-print-none btn-outline-primary" disabled=true}}
  </div>
</div>
<div class="row">
  <div class="col">
    <table class="table">
      <thead>
        <tr>
          <th scope="col" style="width: 20%" class="text-center">
            Monday
          </th>
          <th scope="col" style="width: 20%" class="text-center">
            Tuesday
          </th>
          <th scope="col" style="width: 20%" class="text-center">
            Wednesday
          </th>
          <th scope="col" style="width: 20%" class="text-center">
            Thursday
          </th>
          <th scope="col" style="width: 20%" class="text-center">
            Friday
          </th>
        </tr>
      </thead>
      <tbody>
        {{#each meal_times as |meal_time|}}
          <tr>
            {{#each daysOfWeek as |day|}}
              <td>
                {{new-menu-day openDuplicateModel="openDuplicateModel" viewTransition="viewTransition" weekDestination=weekDestination weekModel=weekModel
                  disableMenu=disableMealSelector mealTime=meal_time dayOfWeek=day mealArray=meals weekSaved=weekSaved}}
              </td>
            {{/each}}
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>
{{new-menu-redirection duplicateModel=duplicateModel errorMessage=errorMessage viewTransition="viewTransition"}}
