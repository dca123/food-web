<div class="row pt-4 pb-2 justify-content-between">
  <div class="col-4">
    <h2>Menu for Week of {{title}}</h2>
  </div>
  <div class="col-3">
  </div>
  <div class="col">
    <button class="btn btn-block btn-lg d-print-none {{if isEditing 'btn-outline-primary' 'btn-outline-success'}}" {{action (toggle "isEditing" this)}}>
      {{if isEditing "View Mode" "Edit Mode"}}
    </button>
  </div>
  <div class="col">
    {{link-to "Compute Shopping List" "menus.list" weekModel.id class="btn btn-block btn-lg d-print-none btn-outline-primary"}}
  </div>
</div>
<div class="row">
  <div class="col">
    <table class="table">
      <thead>
        <tr>
          <th scope="col" style="width: 20%" class="text-center">
            Monday
          </th>
          <th scope="col" style="width: 20%" class="text-center">
            Tuesday
          </th>
          <th scope="col" style="width: 20%" class="text-center">
            Wednesday
          </th>
          <th scope="col" style="width: 20%" class="text-center">
            Thursday
          </th>
          <th scope="col" style="width: 20%" class="text-center">
            Friday
          </th>
        </tr>
      </thead>
      <tbody>
        {{#each meal_times as |meal_time|}}
          <tr>
            {{#each daysOfWeek as |day|}}
              <td>
                {{menu-day entries=model day=day meal_time=meal_time isEditing=isEditing
                  mealArray=meals weekModel=weekModel}}
              </td>
            {{/each}}
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>
